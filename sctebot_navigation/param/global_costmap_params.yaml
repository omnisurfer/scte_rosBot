global_costmap:
  global_frame: odom # change to map if you are using something like AMCL, other odom for "outdoors" unmapped
  robot_base_frame: base_footprint

  update_frequency: 10.0
  publish_frequency: 10.0
  transform_tolerance: 0.5

  width: 50
  height: 50

  static_map: false # default: true
  rolling_window: true
 
